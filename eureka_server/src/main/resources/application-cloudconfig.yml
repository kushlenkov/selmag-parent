server:
  port: 8761
spring:
  application:
    name: selmag-eureka-server
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config:config-password@192.168.0.108:8888